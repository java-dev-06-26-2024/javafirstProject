package com.learning.day3and4.classwork;

public class WorkPlace {
    public static void main(String[] args) {

        // create a Table class   => attributes => length, width , height   => private = done
        // create a object
        // create a methods to access those attributes. = done
        // create a methods to change the values of attributes. =>

        Table table1 = new Table();
        System.out.println(table1.getLength());
        System.out.println(table1.getWidth());
        System.out.println(table1.getHeight());




    }
}
